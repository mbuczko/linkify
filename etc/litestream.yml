dbs:
  - path: /data/${DB_NAME}
    replicas:
      - type: s3
        url: ${REPLICA_URL}
        endpoint: ${S3_ENDPOINT_URL}
        force-path-style: true
        region: eu-central-1
        skip-verify: true
